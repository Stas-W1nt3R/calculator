<Window x:Class="Library.HelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="О программе" 
        Height="300" 
        Width="400"
        Icon="/Library;component/images/icons8-сортировка-32.png"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Научный калькулятор" 
                   FontSize="20" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" 
                   Text="Версия 1.0" 
                   FontSize="14" 
                   Foreground="Gray"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <TextBlock Grid.Row="2" 
                   Text="Программа разработана в рамках лабораторных работ по дисциплине 'Конструирование программного обеспечения'." 
                   TextWrapping="Wrap"
                   TextAlignment="Center"
                   Margin="0,0,0,20"/>

        <Button Grid.Row="3" 
                Content="OK" 
                Width="80" 
                Height="30"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Click="OkButton_Click"/>
    </Grid>
</Window>